import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class FavourScreen extends StatelessWidget {
  const FavourScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Favour'),
        elevation: 3,
      ),
      body: ListView(

        children: const [

        ],
      ),
    );
  }
}
